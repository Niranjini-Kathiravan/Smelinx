# --------------------------------------------------
# Node.js / Package Managers
# --------------------------------------------------
node_modules/           # dependencies
.pnpm-debug.log*        # pnpm debug logs
npm-debug.log*          # npm debug logs
yarn-debug.log*         # yarn debug logs
yarn-error.log*         # yarn error logs

# Yarn Plug'n'Play (if used)
.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# --------------------------------------------------
# Build / Output
# --------------------------------------------------
.next/                 # Next.js build output
out/                   # Next.js static export
build/                 # generic production build folder
coverage/              # test coverage reports
*.tsbuildinfo          # TypeScript incremental build info
next-env.d.ts          # auto-generated by Next.js

# --------------------------------------------------
# Environment / Secrets
# --------------------------------------------------
.env                   # default env file
*.env                  # any env.* variants (e.g., .env.local, .env.production)
smelinx-api/.env       # backend env file
smelinx-web/.env       # frontend env file
smelinx-web/.env.*     # frontend env variants
**/.env
**/.env.*


# --------------------------------------------------
# Local Database & Data
# --------------------------------------------------
smelinx-api/data/      # SQLite DB and app data (keep local only)

# --------------------------------------------------
# OS / Misc
# --------------------------------------------------
.DS_Store              # macOS Finder metadata
*.pem                  # certificates / keys
.vercel                # Vercel deployment metadata

# --------------------------------------------------
# Notes
# --------------------------------------------------
# - Commit only `.env.example` files with placeholder values
# - Keep all real `.env` and DB files out of Git for security

.replacements.txt

# Ignore across the whole monorepo
# ignore everywhere in the repo
**/node_modules/
**/.next/
**/dist/
**/build/
**/out/

